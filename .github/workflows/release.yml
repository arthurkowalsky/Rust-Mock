name: Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  # Publish to crates.io
  publish-crates:
    name: Publish to crates.io
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2

      - name: Publish to crates.io
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: cargo publish --allow-dirty

  # Build binaries for multiple platforms
  build-binaries:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            archive: tar.gz
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            archive: tar.gz
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            archive: tar.gz

          # macOS
          - os: macos-latest
            target: x86_64-apple-darwin
            archive: tar.gz
          - os: macos-latest
            target: aarch64-apple-darwin
            archive: tar.gz

          # Windows
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            archive: zip

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Install cross-compilation tools (Linux)
        if: matrix.os == 'ubuntu-latest' && matrix.target != 'x86_64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          if [ "${{ matrix.target }}" = "x86_64-unknown-linux-musl" ]; then
            sudo apt-get install -y musl-tools
          elif [ "${{ matrix.target }}" = "aarch64-unknown-linux-gnu" ]; then
            sudo apt-get install -y gcc-aarch64-linux-gnu
          fi

      - name: Build binaries
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package binaries (Unix)
        if: matrix.archive == 'tar.gz'
        run: |
          cd target/${{ matrix.target }}/release
          tar czf ../../../mokku-${{ matrix.target }}.tar.gz mokku RustMock
          cd ../../..
          ls -lh *.tar.gz

      - name: Package binaries (Windows)
        if: matrix.archive == 'zip'
        run: |
          cd target/${{ matrix.target }}/release
          7z a ../../../mokku-${{ matrix.target }}.zip mokku.exe RustMock.exe
          cd ../../..
          dir *.zip

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: mokku-${{ matrix.target }}
          path: |
            *.tar.gz
            *.zip
          retention-days: 1

  # Create GitHub Release
  create-release:
    name: Create GitHub Release
    needs: [publish-crates, build-binaries]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release assets
        run: |
          mkdir -p release-assets
          find artifacts -type f \( -name "*.tar.gz" -o -name "*.zip" \) -exec cp {} release-assets/ \;
          ls -lh release-assets/

      - name: Extract version from tag
        id: version
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "version=${VERSION}" >> $GITHUB_OUTPUT

      - name: Extract changelog for this version
        id: changelog
        run: |
          VERSION=${{ steps.version.outputs.version }}
          # Extract section for this version from CHANGELOG.md
          awk "/## \[${VERSION}\]/,/## \[/" CHANGELOG.md | sed '1d;$d' > release-notes.md
          if [ ! -s release-notes.md ]; then
            echo "Release notes for version ${VERSION}" > release-notes.md
            echo "" >> release-notes.md
            echo "See [CHANGELOG.md](CHANGELOG.md) for details." >> release-notes.md
          fi
          cat release-notes.md

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: Release v${{ steps.version.outputs.version }}
          body_path: release-notes.md
          files: release-assets/*
          draft: false
          prerelease: false
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate installation instructions
        run: |
          VERSION=${{ steps.version.outputs.version }}
          echo "## 📦 Installation" >> install-instructions.md
          echo "" >> install-instructions.md
          echo "### Via crates.io (Recommended)" >> install-instructions.md
          echo "\`\`\`bash" >> install-instructions.md
          echo "cargo install RustMock" >> install-instructions.md
          echo "\`\`\`" >> install-instructions.md
          echo "" >> install-instructions.md
          echo "### Via Binary Download" >> install-instructions.md
          echo "" >> install-instructions.md
          echo "**Linux (x86_64 GNU):**" >> install-instructions.md
          echo "\`\`\`bash" >> install-instructions.md
          echo "curl -L https://github.com/arthurkowalsky/Rust-Mock/releases/download/v${VERSION}/mokku-x86_64-unknown-linux-gnu.tar.gz | tar xz" >> install-instructions.md
          echo "sudo mv mokku /usr/local/bin/" >> install-instructions.md
          echo "\`\`\`" >> install-instructions.md
          echo "" >> install-instructions.md
          echo "**macOS (Apple Silicon):**" >> install-instructions.md
          echo "\`\`\`bash" >> install-instructions.md
          echo "curl -L https://github.com/arthurkowalsky/Rust-Mock/releases/download/v${VERSION}/mokku-aarch64-apple-darwin.tar.gz | tar xz" >> install-instructions.md
          echo "sudo mv mokku /usr/local/bin/" >> install-instructions.md
          echo "\`\`\`" >> install-instructions.md
          echo "" >> install-instructions.md
          echo "**Windows:**" >> install-instructions.md
          echo "Download \`mokku-x86_64-pc-windows-msvc.zip\` and extract to your PATH" >> install-instructions.md
          cat install-instructions.md

      - name: Comment installation instructions
        run: |
          echo "✅ Release v${{ steps.version.outputs.version }} created successfully!"
          echo ""
          echo "Binaries available for:"
          ls -1 release-assets/ | sed 's/^/  - /'
          echo ""
          echo "Published to crates.io: https://crates.io/crates/RustMock"
